# GOT-OCR2.0 dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.0.0
transformers>=4.46.0
accelerate>=0.26.0

# GOT-OCR2.0 specific
tiktoken
verovio
sentencepiece

# Image processing
Pillow>=9.0.0

# PDF support (requires poppler installed separately)
pdf2image>=1.16.0

# Optional: faster inference
# flash-attn  # requires CUDA, install separately if needed
