/**
 * Docker Compose Configuration
 *
 * NutriMind development and production deployment
 */

version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: nutrimind-postgres
    environment:
      POSTGRES_USER: nutrimind
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nutrimind123}
      POSTGRES_DB: nutrimind
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nutrimind -d nutrimind"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nutrimind-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: nutrimind-redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nutrimind-network

  # Neo4j Knowledge Graph
  neo4j:
    image: neo4j:5.12-community
    container_name: nutrimind-neo4j
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-nutrimind123}
      NEO4J_PLUGINS: ["apoc", "graph-algorithms"]
      NEO4J_dbms_memory_heap_initial__size: 512m
      NEO4J_dbms_memory_heap_max__size: 2g
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    ports:
      - "7474:7474"
      - "7687:7687"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7474"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nutrimind-network

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nutrimind-backend
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgresql://nutrimind:nutrimind123@postgres:5432/nutrimind
      REDIS_URL: redis://redis:6379
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:-nutrimind123}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy
    networks:
      - nutrimind-network
    restart: unless-stopped

  # Frontend (if building separately)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nutrimind-frontend
    environment:
      API_BASE: http://backend:3001/api/v1
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - nutrimind-network
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  neo4j_data:
  neo4j_logs:

networks:
  nutrimind-network:
    driver: bridge
